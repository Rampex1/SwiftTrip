<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SwiftTrip" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SwiftTrip" parent="Base.Theme.SwiftTrip" />

    <style name="Theme.SwiftTrip.Splash" parent="Theme.SplashScreen">
        <!-- Force our own custom background -->
        <item name="windowSplashScreenBackground">@drawable/splash_background</item>

        <!-- Explicitly tell Android NOT to apply dynamic colors (which causes gray overlays) -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- No icon -->
        <!-- <item name="windowSplashScreenAnimatedIcon">@drawable/full_logo</item> -->

        <!-- Return to normal app theme -->
        <item name="postSplashScreenTheme">@style/Theme.SwiftTrip</item>
    </style>

</resources>